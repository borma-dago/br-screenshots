{
 "A1": [
  ["Option 1 — code-read → column, staff-authored → attribute", "Our code already implements it; every code-read fact is a column except the four dimensions.", "Unstable in one direction: the day a rule reads <code>rasa</code>, “code-read” says promote it — a migration per new consumer. No rule for the <code>ProductMeta</code> home. Its answer for net content (26,317 titles) is “author a new attribute”: correct, unscheduled.", "Shopify · Google · eBay · Walmart · Shopee · Tokopedia · Salesforce · WooCommerce · commercetools (9)"],
  ["Option 2 — everything is an attribute", "Coherent; one registry.", "Wrong at our size: <code>ProductAttributeValue</code> has no locale and no scope column, and a title is per-locale for us (Amazon: <code>selectors [marketplace_id, language_tag]</code>; Akeneo: <code>{locale, scope, data}</code>). Not “move a column”, but “rebuild the value table, then rewrite ~20 core call sites” — and it mutates a live <code>through=</code> model on <code>Product.attributes</code>. The two platforms that do it keep scalar copies anyway.", "Amazon (write path) · Akeneo · Magento"],
  ["Option 3 — capped custom fields", "A governance knob.", "Binds nothing today (1 definition after condition 2) and contradicts Lock 1 tomorrow (639 nodes authoring schemas) unless re-expressed as a per-category cap — a step-1 decision.", "Square (cap scope unresolved)"],
  ["Option 4 — one registry over both storages (<code>storage = value_row | column</code>)", "Option 1's instability disappears (promotion = a <code>storage</code> flip + a data move); condition 2 stops deleting four definitions' metadata; A0's flags become uniform; the label change is small (<code>name</code> is already the label of record).", "One column on <code>ProductAttribute</code> + a serializer resolver; no data moves. Risk: a registry that describes columns can drift from them — needs a test that every <code>storage='column'</code> row names a real field. Does not fix propagation by itself (<code>ProductAttributeValue</code> has no <code>post_save</code>; <code>models.py:489</code> enqueues before <code>:490</code> writes).", "Magento (<code>backend_type='static'</code>); Akeneo keeps <code>identifier</code> as both a column and an attribute"]
 ],
 "A2": [
  ["Shape 1 — one object, role flag (+ a per-family dimension row)", "One write path, one editor, one filter path. Ordering and a closed list come from rows (A4), not from a second object — Magento gets both inside one object.", "The derived <code>variant_key</code>: a DB invariant becomes a derived column recomputed on a hook that fires on every <code>Product.save()</code> (<code>models.py:486-491</code> → <code>product_attributes.py:60-64</code>). “Don't show the axis twice” becomes a flag rather than a structural fact (handed to A0). Salesforce's migration away from this shape is the counter-evidence (value lists on a shared definition — A3/A4 must scope the list).", "Amazon · Walmart · Akeneo · WooCommerce · Magento (5); eBay both at once"],
  ["Shape 2 — two objects (option beside attribute)", "Value ordering and a closed list are ready-made — but those are benefits of rows, not of two objects.", "Spends the budget away from where our pain is measurable (after condition 2: one definition, 46,499 values, 33 % junk — untouched). Two editors, two filter paths (Square), weaker typing on the axis side (Shopee), and a bridge back (Shopify's <code>linkedMetafield</code>, “early access”).", "Shopify · Shopee · Square · Salesforce (4)"]
 ],
 "A0": [
  ["Option 1 — purpose flags on the definition", "Fits <code>ProductAttribute</code> with no new table (it already carries <code>type</code>, <code>required</code>); reaches the staff app through the existing definition endpoint (<code>staff_views.py:57-75</code>). Replaces three things that are already wrong: the spec-sheet deny-list, the staff-form priority array, the backend alphabetical sort, and the six hard-coded i18n keys.", "Three changes on two stacks, not one. Cannot stop the spec sheet rendering junk (a flag says “show this attribute”, not “this value is meaningful” — A4's). Feed and filter flags are declarations of intent until <code>ProductAttributeValue</code> gets a receiver and the save-ordering bug is fixed.", "10 of 13"],
  ["Option 2 — per category membership", "—", "Not a visibility mechanism anywhere in the thirteen: the category decides membership (A3's question). A0 should not spend a flag on it.", "0 as visibility; 8 as membership"],
  ["Option 3 — per channel", "—", "Our three channels (<code>is_offline_only</code>, the feed) are product-level gates today (<code>models.py:501-515</code>, <code>managers.py:19-26</code>) — where Google, Tokopedia and Square put theirs. Per-channel <em>attribute</em> visibility would be the only instance outside Shopify, whose own history is a five-valued enum reduced to two writable values in three years.", "Shopify (1)"]
 ],
 "A3": [
  ["(a) global type + membership table", "Values survive a product's category move; one filter across categories (materialises for Color 11,730 / Pattern 11,686 / Material 4,546 edges — not for flavour or size, even on Shopify); one rename; one list to curate. Our reuse is head-heavy (<code>net_content</code> 617 nodes, <code>colour</code> 494, <code>flavour</code> 199).", "If the new category does not subscribe, the value row becomes an <b>orphan that still renders</b> — <code>validate_attributes()</code> never sees it, <code>ProductDetailsSerializer</code> serialises <code>attribute_values.all()</code> unfiltered (<code>serializers.py:207</code>), cached by <code>ProductFullViewSet</code>. Fixable with the move-guard (condition 5) + a filtered read. Also: <code>required</code> moves to the membership (D17 early), <code>UNIQUE(code)</code> on data that never had it, the 24-hour attribute cache (<code>staff_views.py:73</code>) becomes a correctness bug.", "Amazon · Shopify · Salesforce · Akeneo · Magento · Tokopedia B (6; 4 unhedged)"],
  ["(b) per-category copy — Lock 1's own “concretely” sentence", "Smallest migration, no new table, simplest editor.", "<b>Mis-attribution on a move</b>: a value row points at a category-scoped attribute, so moving the product leaves every value referencing the old category's attribute; nothing catches it and the value still renders — correct handling is a re-point of every value row on every staff move. Nothing stops <code>flavor</code> being <code>text</code> on one node and <code>integer</code> on another; Elasticsearch needs one mapping per field name, so the first divergence breaks any cross-category facet.", "eBay · Walmart · commercetools (3)"],
  ["(c) shared type, per-category value list", "Answers “noodle flavours ≠ snack flavours” — the case the industry answers with (a) plus a second type.", "Three new tables, a two-level editor built before any attribute editor exists, a second place a value can be wrong (offered-then-withdrawn options).", "0 — nobody ships pure (c)"],
  ["(a′) global type + per-membership <code>name_override</code>", "“Rasa” on food and a different label elsewhere from one id, one list, one filter; working precedent at 14,606-category scale (Shopify's 316 extended attributes reuse the base GID).", "One nullable column. Does <b>not</b> solve “noodle flavours ≠ snack flavours” — only a second type does.", "Shopify"]
 ],
 "A4": [
  ["(a) free string + normalisation on write (#10778 V6)", "No new table — its whole case.", "Fixes 1.6 % of the duplication (5,580 → 5,489), leaves the 16 spellings of <code>PT UNILEVER INDONESIA TBK</code>, is irreversible, cannot give ordering or a rename, cannot make the variant key a DB constraint.", "Amazon · eBay · Google · Walmart (4)"],
  ["(b)/(c) option row — <code>ProductAttributeOption(attribute_id, code, label_en, label_id, display_order, amount, unit)</code> + <code>value_option</code> FK", "<code>_get_value/_set_value</code> already dispatch on <code>\"value_%s\" % attribute.type</code>; both renderers keep working if <code>_option_as_text</code> returns the localised label; the variant key becomes <code>UNIQUE (group_id, option_a_id, …)</code>; a rename is one row; a cross-category filter is a join over rows (Square ships the query). <code>label_en/label_id</code> is our own <code>Category</code> pair, and Magento's/Salesforce's split.", "The write path: <code>value</code> on the wire becomes an option id (<code>staff_serializers.py:38</code>, <code>product-attribute.model.ts:21</code>, the formly type map). Store a real FK, not a code — Akeneo copies the code and gets “referential integrity is a validator, not a foreign key”. One list holds every category's values (the industry answer is a second type). <code>manufacturer</code> stays <code>text</code> on day one.", "Magento · Square (rows); hybrid: Shopify · Shopee · Tokopedia · Salesforce · Akeneo · WooCommerce"],
  ["List on (B) the category / (C) the family", "(C) is right as the home of a <em>selection</em> (D5), not of the list.", "(B) needs a fourth table and a second place a value can be wrong; nobody ships pure (B). (C) at 106,161 products — most families of one — makes the option table larger than the value table, no two families share a value, nothing to filter, rename or order; Shopify bolts sharing back on (linked values).", "(B) 0 pure; (C) Shopify unlinked, WooCommerce local"],
  ["Quantities: (ii) unit fixed on the definition", "What we already ship (the unit is in <code>ts/assets/i18n/product/*.json</code>); a <code>unit</code> column completes it.", "Breaks when one attribute carries values in more than one unit — <code>SOKLIN SOFTERGENT</code> holds <code>215 GR · 720 GR · 1.44 KG · 2.7 KG</code> and a <code>700ML</code> refill — and Permendag 31/2011 Lampiran II makes the printed unit a function of the magnitude (<code>1000 g ≤ Qn → kg</code>). Legally unavailable for net content.", "Salesforce · Square · Shopify (bare, unit elsewhere)"],
  ["Quantities: (iii) amount + unit on the value (Akeneo metric) / (iv) label as record, magnitude derived on write", "(iii) handles multi-unit lists; Akeneo defaults the unit on the definition and overrides on the value — the shape our law requires. (iv) does the same without two fields: staff type <code>1.44 KG</code>, a write-time parse stores <code>amount = 1440, unit_canonical = 'g'</code>; all 886 labels parse, 0 fail; correct on the 782 of 1,993 size families text order gets wrong; 0 curation vs 886 hand-set positions.", "Honest bill: one table, one column, one enum entry, one validator, one type-alias member, one API field, one form type. Untested on staff-authored labels (<code>1 LUSIN</code>, <code>1/2 KG</code>).", "Akeneo · Walmart · Shopee · Tokopedia B (typed + unit); Open Food Facts' computed <code>product_quantity</code> for (iv)"]
 ],
 "A5": [
  ["A5-a — pinned on the definition (<code>ProductAttribute.level</code>)", "<code>SameForAll</code> never needs enforcing — “the attribute is on the family” <em>is</em> the constraint (commercetools implements it as an error type because its slot cannot carry it). Day-one cost zero: <code>level</code> defaults member, <code>variant_group_id</code> NULL on all 471,143 rows.", "<code>validate_attributes()</code> must learn the level in the same commit that moves the first definition — two halves: skip family-level definitions in the member loop <b>and</b> validate them on the group, and no family model, <code>clean()</code> or write path exists yet. Staff-form read/write asymmetry: a merged read + an unsplit write puts a family value on a member, and the partial unique index will not catch it.", "Shopify · Square · Tokopedia B · commercetools (declared); Shopee · WooCommerce (by structure)"],
  ["A5-b — chosen per family", "Akeneo's flexibility.", "Lock 2 creates 106,161 groups on day one — 106,161 distributions or a default, and the default is A5-a under another name. Akeneo's distribution lives on <code>FamilyVariant</code>, a reusable structure whose analogue for us after Lock 1 is the <b>Category</b>, not the group — the faithful port is a level on the <code>(category, attribute)</code> row, i.e. A5-a. No precedent at the family level in the thirteen.", "Akeneo (on FamilyVariant)"],
  ["A5-c — always on the member, copies across siblings", "It is today.", "Today's drift is measured: 18.9 % of proxy families disagree on <code>manufacturer</code>. The three precedents replicate because they have no container; doing it under a container is the one combination none of the thirteen runs.", "Amazon · Google · Walmart · Magento (no container content row)"],
  ["A5-d — both levels, read-time fallback", "Already taken for images (member first, family fallback) — a presentation fact with most-specific-match semantics (Salesforce).", "Breaks the card's rule (“if on the product, its variants do not store it”); for a data fact the second copy is a second truth; unifying would put precedence logic in every reader for no case our data shows. Keep images and attributes on different rules on purpose.", "Salesforce (primary); WooCommerce stock, Shopee weight/dimension (as a mechanism)"]
 ],
 "C1": [
  ["C1-i — strict, policed", "Same answer as ii in a different architecture.", "Replication + admission control is what you build when there is no container row — the “must be identical” rules are the container expressed as a validator. We will have a container.", "Amazon · eBay · Walmart · Meta (flat-feed platforms)"],
  ["C1-ii — family-owned content, member-owned commerce", "Divergence is unrepresentable — the cheapest enforcement is an absent column. Clause by clause: one family per member (DB, <code>NOT NULL</code>); own barcode (DB, <code>upc unique</code>); shared category (DB, composite FK on <code>(variant_group_id, main_category_id)</code>); unique axis combination (DB, condition 4's mirror); same axis names (by construction); family owns the description and the customer-facing title; member owns a short label.", "The description migration: 3,912 of 10,090 proxy families have a different description on every member — at merge the screen must present every losing description and require a choice, never discard silently. The composed title touches nine consumers across four apps incl. a shared search template. Brand stays advisory (unenforceable on today's data; 25.8 % of name-proxy families straddle categories).", "Shopify · Shopee · Tokopedia B · Square · commercetools (5)"],
  ["C1-iii — free", "Per-selection prose (WooCommerce swaps a per-variation description block).", "Inherited schema, not a decision: both platforms' container is an ordinary row/post that already had those columns. Our Indomie drift is the case against it.", "Magento · WooCommerce"]
 ],
 "C2": [
  ["C2-a — count is an axis (consumer multipack)", "GS1 §2.3: same hierarchy level as the single, new GTIN, consumer expected to distinguish. Amazon and Walmart name it; eBay pivots on it (gap pass: <code>Number in Pack</code> 12/12 grocery leaves); nobody forbids it. Walmart's own data: count is the only axis a toothpaste and a tyre share.", "Feed: under C2-a the pack and the single share an <code>item_group_id</code> and Google's variant rules apply — but the exporter's suppression branch is the common path: of 3,633 multipack rows ~2,900 have no single. Per-unit price comparability not checked (one <code>PriceSell</code> join, not run).", "Amazon · Walmart (named); 10 expressible; eBay (gap pass)"],
  ["Carton / <em>dus</em> — not a product", "GS1 §2.8: higher packaging level, <code>N/A</code> at the consumer level. Already <code>ProductMeta.quantity_per_box</code>.", "None — it is what we do.", "GS1"],
  ["C2-c — banded pack of different products: separate, no family link to its components", "By C1's own rule: it differs from the plain product in two things at once (it contains a second product), so no single axis value names the difference. GS1 §2.9: the assortment is its own trade item, components carry their own GTINs. Shopee's kit and Amazon's <code>PACKAGE_HIERARCHY</code> are containment edges, not sibling links — nothing found that makes a bundle a sibling of its component.", "The bundle may itself be a family (a shampoo+conditioner kit in two scents); ours has one form → a family of one. #10778 M4 re-expressed: true for <code>2×500 ml</code>, not for #11126's Bango-plus-sachet.", "Amazon (PRODUCT_BUNDLE / PACKAGE_HIERARCHY) · Walmart (Virtual Pack) · Google (is_bundle) · Shopee (kit) · Tokopedia B (combined_skus)"],
  ["“Sachet vs bottle” = form, a different axis from count", "Form is a property of one trade item at one level; count is declared net content at that level. Both can be axes — two axes, so a 6-sachet pack vs a single bottle is a two-axis grid cell. Shopify carries <code>Package type</code> as a choice list (gap pass).", "D6's cap of 3 must hold flavour × net content × pack form.", "GS1; Shopify (Package type)"]
 ],
 "C3": [
  ["C3-a — one level, by not building the column", "Expressed by the absence of a column, not a validator — nesting cannot be represented, let alone violated (the same argument that made shape B beat C). Adding <code>parent_id</code> later is a no-op migration (arrives NULL everywhere). Two axes cover every grid our data contains; D6's cap of 3 leaves headroom for flavour × net content × pack form.", "Removing it later would not be a no-op (collapsing staff-authored hierarchies, re-homing every member).", "9 of 13"],
  ["C3-b — two levels", "A home for content that belongs to a colour across sizes (Akeneo's reason); an addressable subset URL (Salesforce's reason).", "Our grids are ragged (Indomie: one flavour at two sizes, 31 at one) and 71 % of products have no image, so Akeneo's reason does not apply; the family has no URL at all today. Salesforce's version is Beta-gated and disables slicing.", "Akeneo · Salesforce (both capped at 2)"]
 ],
 "B1": [
  ["(a) two kinds — folders and types", "A stored, explicit policy.", "Makes <em>giving a type a child</em> an illegal state — our tree grows exactly that way (<code>Alat dan Buku Tulis &gt; BUKU TULIS</code>: 219 products on the parent and a child of the same name; <code>SAYURAN SEGAR</code> grew to 90 children keeping 222 products). Stores <code>numchild == 0</code> twice; requires resolving all 292 used internal nodes before the migration can run.", "Square (function kinds, not folder/type)"],
  ["(b) one kind; leafness derived", "Costs nothing.", "Changes nothing — including what Lock 1 needs changed: the three non-product roots (<code>UNUSED</code> 1,923, <code>BELUM DISORTIR</code> 1,401, <code>INVENTORY KANTOR</code> 78) become product types with schemas.", "8 of 13"],
  ["(c) one kind + per-node <code>accepts_products</code>", "A column add with a default and no data change — the only property that lets it ship before the 19,522 are cleaned; enforceable at the one write path; incremental clean-up.", "<code>accepts_products</code> and <code>numchild</code> can disagree; the switch records a policy with no reason attached (the 292 still need the classification first); needs a staff surface that does not exist — the admin Category List is a flat paginated table (<code>category-list-staff.component.ts:27</code>).", "0 at node scope; Akeneo at tree scope"]
 ],
 "B2": [
  ["(a) any node", "A fit-check pass; costs nothing.", "Leaves 292 nodes owning schemas more general than their children's, and the three non-type roots owning schemas.", "8 of 13"],
  ["(b) leaves only", "Every per-category policy keyed on one leaf id (eBay's reason).", "Forces a home for 19,522 products <em>before Lock 1 ships</em>; the 21.9 % residual types force us to invent a leaf per facet-parent (<code>KACAMATA BIASA</code>, <code>MINISET</code>, <code>KANCING</code>) — the eBay seller behaviour, reproduced by design (we already have 44 such used nodes). Makes <code>add_child</code> illegal on any node holding products, which is how our tree has grown. Cannot help the 3,402 products whose root is not a type.", "eBay · Shopee · Tokopedia B"],
  ["(c) the switch, defaulted True", "Zero-row migration; enforceable at the one write path; incremental clean-up.", "A second field that can disagree with <code>numchild</code>; a staff surface that does not exist yet.", "0"]
 ],
 "B3": [
  ["Criterion: a distinct attribute set", "—", "Fails as a node criterion: 1,686 nodes carry 68 distinct measured sets; 98.8 % duplicate another node — this would earn ~68 nodes.", "—"],
  ["Criterion: a browse need", "The only criterion our depth currently serves.", "The UI is a one-level-at-a-time drill-down with a one-hop parent link and no full trail; each level costs a tap going down.", "—"],
  ["Criterion: a reporting need", "Already satisfied at any depth: the ES <code>category</code> field carries every ancestor id, so all nine read/filter <code>category-id</code> registrations match a whole subtree from one id.", "—", "—"],
  ["Criterion: a minimum product count", "The only one that discriminates: 609 used nodes hold &lt; 10 products, 216 hold &lt; 3. Priced: “≥ 10 or merge into the parent” merges 609 nodes and moves 2,545 products, leaving 1,077 used nodes; at 3: 216 nodes, 315 products. The 206 unused nodes are free to delete.", "Neither threshold is a recommendation — both are the price tag. There is no staff delete endpoint (<code>CategoryStaffViewSet</code> has Create + Update only; deletion is Django-admin-only).", "—"]
 ],
 "B4": [
  ["(a) nothing flows", "The simplest editor (one screen, one list). A category move changes nothing — under (b) a move silently rewrites the effective schema of every product in the subtree, and <code>api/apicategory/staff_serializers.py:104-148</code> validates nothing beyond <code>full_code</code> uniqueness.", "The largest authoring bill (1,686). What the eleven ship — all of which author centrally or have no tree on the attribute owner.", "11 rows"],
  ["(b) inherit, add-and-override, never remove", "The only option whose bill is bounded by 68 (distinct sets), not 1,686. Copy Akeneo's restriction (inherit-and-add, override <code>required</code>, never remove) and the tombstone disappears from the schema entirely.", "The effective set must be computed or materialised (three ways, priced in <code>design-B4.md</code> §2); the move becomes a schema migration needing a pre-flight; the editor must show <em>inherited from X</em> vs <em>own</em>.", "Salesforce (category tree, single route); Akeneo (Family tree, SaaS)"],
  ["(c) root set + leaf additions", "Matches today's data exactly (one class, five universal attributes).", "Cannot express <code>Food → Rasa</code> on a four-level branch; Google is the nearest precedent and it degrades into an enumerated prose exception list.", "0 (Google, partially)"],
  ["(d) per kind of fact", "Our code already carries six kinds of category-derived fact resolved by four mechanisms; eBay does exactly this (aspects no, features yes).", "Each kind needs its own stated rule: definitions (b); requiredness inherits, child may tighten; values inherit, child may narrow; age-walling must flow by ancestry (today's exact-id list is a live bug); replenishment and <code>is_public</code> already flow; <code>accepts_products</code> must not.", "eBay"]
 ],
 "FORK": [
  ["(a) keep authoring our own tree", "1,892 nodes stay; 609 sub-10-product nodes and 206 unused nodes are ours to prune; population = the 8 attribute kinds our titles already carry; we own every change, no external clock; no mapping; our code untouched.", "Schema authoring is a range: 68 sets measured floor · 316 inferred over the mapped share (220 conservative) · 1,686 under D15=(a) — bounded by sets only if B4 = (b). Net content / size ours to design (A4).", "0 of 13 adopt another's tree; 6 merchant tools are silent"],
  ["(b) adopt the Shopify Standard Product Taxonomy", "Zero schema authoring — 93,007 edges arrive materialised; 8,240 names with closed <code>id-ID</code> lists.", "Re-home 1,686 used nodes: ~74 % need hand mapping and a quarter to a third of the automated ones are wrong (Wilson CI 14–50 %); no node for instant noodles, Batik, Mukena, Sajadah, Sambal, Terasi (~2,800 products); 302 gender/age nodes need a restructuring decision; population = 776 distinct names over 434 mapped nodes (558 conservative) that nothing in our data fills; net content <b>not solved</b>; Shopify's cadence (<code>dist/</code> retired 2026-10-31; two artifacts disagree on <code>level</code>; 18 duplicate id-ID paths); six keyed things re-keyed, every category document rewritten; 0 of 73 Indomie flavour tokens in the 30-value Flavor list.", "0"],
  ["(c) keep our tree, hold a mapping", "Buys its way out of re-homing and re-keying; the taxonomy becomes a <em>source</em> for the attribute catalogue (V1 seed); one nullable column.", "The same our→Shopify mapping <b>in full</b> — 1,252 nodes unmapped mechanically, a quarter to a third of the 434 automated wrong, 302 gender/age nodes. Shopify's 14,528 + 5,754 published rules take a <em>Shopify</em> category as input (the hop after ours) and relieve none of it; re-mapping on every release is a standing cost the vendor itself pays (5,595 rules from <code>2022-02</code> alone). Nothing consumes a Shopify id today — the Google feed builder never touches category.", "Shopify → Google (mapping); Tokopedia B (dual tree) — n = 2"]
 ]
}
