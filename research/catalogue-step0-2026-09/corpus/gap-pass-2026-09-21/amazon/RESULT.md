# Amazon — first-party doc re-verification via `llms.txt` + `.md` · gap pass 2026-09-21

**Route.** #11068 §3.1: `curl -sL https://developer-docs.amazon.com/sp-api/llms.txt` (361,401 B index) and
`…/sp-api/docs/<slug>.md` / `…/sp-api/reference/<slug>.md`. Tier-1 first-party text, no credential, one route.
Every page carries `updatedAt: 2026-09-09/10`. The live per-type JSON Schema (`getDefinitionsProductType`
against a real type) stays credential-gated; nothing here is a live enum.

| File | Bytes | What it verifies (record claim → status) |
|---|---|---|
| `product-type-definition-meta-schema.md` | 6,702 | The meta-schema vocabulary, whole: `editable` (Informational — *"Indicates whether or not a property value can be modified for an existing item. Properties that can't be modified may still be required for a valid submission."*), `enumNames` (*"display labels … in the same order as the values in the enum array"*), `hidden` (*"Determines if a property should be hidden in Amazon user interfaces. Hiding or displaying these properties is at the discretion of the application consuming the … Meta-Schema."*), `maxUniqueItems` / `minUniqueItems` (Validation, *"Use in conjunction with selectors"*), `maxUtf8ByteLength` / `minUtf8ByteLength`, `selectors` (*"an array of property names that define the combination of properties that make an object unique"*), `$lifecycle` / `replacedBy` / `replaces` / `enumDeprecated`. **#10976 carried `hidden`/`editable` from a SUPERSEDED comment and P2/P3 dumps (Tier-3 warning); now first-party.** Feeds A0 (V1 flag on the definition). |
| `catalog-items-api-v2022-04-01-reference.md` | 71,479 | `Type` enum, exactly two: `VARIATION` (*"is a variation parent or variation child of the related items"*), `PACKAGE_HIERARCHY` (*"is a package container or is contained by the related items that are identified by ASIN"*) — confirms #10976 §1.8. **New:** `ItemClassification` enum, four values: `BASE_PRODUCT` (*"can be directly purchased. Can be a standalone ASIN or a variation child item"*), `OTHER`, **`PRODUCT_BUNDLE`** (*"A parent catalog item that represents a bundle of items"*), `VARIATION_PARENT` (*"A parent catalog item that groups child items into a variation family"*). #10976 had only observed `BASE_PRODUCT` and named `VARIATION_PARENT`; `PRODUCT_BUNDLE` is a **third parent kind beside VARIATION and PACKAGE_HIERARCHY** — a bundle is a classified catalogue item, not a relationship. `ItemVariationTheme`: `attributes` *"Names of the Amazon catalog item attributes that are associated with the variation theme"* (`< string > array`), `theme` example `"COLOR_NAME/STYLE_NAME"` (string, no enum) — confirms A2's "names not $refs" and "bare string, no enum key". Feeds A2, C2. |
| `submit-listings-data.md` | 22,114 | *"A variation family represents a collection of items for one product that is sold in multiple varieties … Each unique size and color combination is represented by a separate listings item and grouped together by a parent listings item to provide a single detail page experience."* and *"a parent listings item is created first and each child listings item refers to the parent listings item with the `child_parent_sku_relationship` attribute. Each listings item in the variation family uses the `parentage_level` attribute to define if it is a parent or child … and the `variation_theme` attribute to define the variated attributes"* — the three ordinary attributes of A2, stated first-party; the stated **purpose** of the family is *"a single detail page experience"*. Feeds A2, C1 (why). |
| `reference-getdefinitionsproducttype.md` | 33,044 | The static sandbox response: `propertyGroups.product_identity {title, description "Information to uniquely identify your product (e.g., UPC, EAN, GTIN, Product Type, Brand)", propertyNames [item_name, brand, external_product_id, gtin_exemption_reason, merchant_suggested_asin, product_type, product_category, product_subcategory, item_type_keyword]}` — confirms matrix-A1's Amazon row (title and brand are properties of the type definition) from the first-party reference. Parameters: `requirements ∈ {LISTING, LISTING_PRODUCT_ONLY, LISTING_OFFER_ONLY}`, `requirementsEnforced ∈ {ENFORCED, NOT_ENFORCED}` (*"Non-enforced requirements enable structural validation of individual attributes without all the required attributes being present (such as for partial updates)"*), `sellerId` (*"seller-specific requirements and values are populated … such as brand names associated with the selling partner"*), `locale`. Feeds A1, D17/D18. |
| `reference-getcatalogitem.md` | 92,338 | Reference for `includedData=relationships` etc. — fetched, not yet mined. |
| `mapping-product-attributes.md`, `listings-items-guidance-for-complex-attributes.md`, `submit-modular-titles.md`, `retrieve-a-product-type-definition.md`, `product-type-definitions-api.md` | 5–15 KB each | fetched for completeness; not mined in this pass. |

**Not settled (unchanged):** the live per-type `variation_theme` enum, the JSON-side `type/constraints/enum` on an attribute (❓UNKNOWN in #10976 §1.3), the "non-varying attributes must match" rule's first-party page (that quote is from Seller Central help, not SP-API docs — not in `llms.txt`).
